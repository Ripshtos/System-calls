This script dynamically check the student directory for files, runs the c code inside of them and saves the output to a txt file, then compares the text file based on what the user wrote 
in the expectedOutput, then automatically grades the code after checking its output inside the results csv file.
